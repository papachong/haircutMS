# Database Configuration
POSTGRES_USER=haircutms
POSTGRES_PASSWORD=changeme_in_production
POSTGRES_DB=haircutms

# Redis Configuration
REDIS_PASSWORD=changeme_in_production

# Server Configuration
NODE_ENV=production
PORT=4000

# JWT Configuration - Generate strong secrets with: openssl rand -base64 32
JWT_SECRET=changeme_in_production_generate_strong_secret
JWT_REFRESH_SECRET=changeme_in_production_generate_strong_secret

# Docker Image Names (for docker-compose)
WEB_IMAGE=ghcr.io/your-username/haircut-ms/haircutms-web:latest
SERVER_IMAGE=ghcr.io/your-username/haircut-ms/haircutms-server:latest