# 理发店管理系统 — 开发计划与任务拆分

## 阶段总览

```
Phase 0: 基础架构           Week 1-2    前置，所有功能依赖此阶段
Phase 1: 会员 + 充值         Week 3-5    核心数据基础
Phase 2: 服务项目 + 收银     Week 6-8    核心交易流程
Phase 3: 数据分析 + 员工     Week 9-10   经营洞察
Phase 4: 平台管理 + License  Week 11-12  多租户管控
Phase 5: 联调 + 上线准备     Week 13-14  集成验证
```

---

## Phase 0：基础架构（Week 1-2）

> 所有后续功能的前置依赖，搭建项目骨架和通用能力。

### T0.1 初始化 Monorepo 脚手架

**目标：** 项目可启动、可构建、可本地开发

| 子任务 | 说明 |
|--------|------|
| 初始化 pnpm workspace + Turborepo | `haircutMS/` 下 `apps/` `packages/` 目录结构 |
| 初始化 `apps/web` (Next.js 15) | App Router、Tailwind CSS、shadcn/ui、响应式布局基础 |
| 初始化 `apps/server` (NestJS) | 模块目录结构、统一响应拦截器、异常过滤器 |
| 初始化 `packages/shared` | TypeScript 类型、常量枚举、Zod 校验 schema、金额工具函数 |
| 配置 ESLint + Prettier | 统一代码风格 |
| 配置 Docker Compose | PostgreSQL 16 + Redis 本地开发环境 |

**验收标准：**
- [ ] `pnpm dev` 可同时启动前端和后端
- [ ] 前端访问 `http://localhost:3000` 能看到页面
- [ ] 后端访问 `http://localhost:4000/api/v1/health` 返回 `{ code: 0 }`
- [ ] Docker Compose 启动 PostgreSQL 和 Redis，后端可连接

**产出文件：**
- `package.json`, `pnpm-workspace.yaml`, `turbo.json`
- `apps/web/`, `apps/server/`, `packages/shared/`
- `docker/docker-compose.yml`

---

### T0.2 数据库模型 + 迁移

**目标：** 所有 Prisma 模型就位，数据库可迁移

| 子任务 | 说明 |
|--------|------|
| 编写完整 Prisma Schema | 参照技术方案 5.2 节全部模型 |
| 生成初始迁移 | `prisma migrate dev` |
| 编写 seed 脚本 | 平台管理员 + 测试店铺 + 测试员工 + 测试会员 |
| 金额工具函数 | `packages/shared/utils/currency.ts` |

**验收标准：**
- [ ] `prisma migrate dev` 成功创建所有表
- [ ] `prisma db seed` 可插入测试数据
- [ ] 所有模型关系正确（Shop → Staff → OrderItem 等外键链路）

**依赖：** T0.1

---

### T0.3 认证模块

**目标：** 员工可登录、JWT 签发、请求鉴权

| 子任务 | 说明 |
|--------|------|
| Auth Module (NestJS) | login / refresh / logout |
| JWT 签发 | Access Token (15min) + Refresh Token (7天, Redis) |
| bcrypt 密码加密 | 注册/修改密码 |
| Auth Guard | 全局守卫，从 JWT 提取 staffId/shopId/role |
| 登录页面 (Next.js) | 手机号 + 密码，选择/识别所属店铺 |
| Token 刷新机制 | 前端 axios 拦截器自动刷新 |

**验收标准：**
- [ ] POST `/api/v1/auth/login` 返回 accessToken + refreshToken
- [ ] 携带过期 Token 请求返回 401
- [ ] POST `/api/v1/auth/refresh` 可换取新 Token
- [ ] 前端登录后跳转到管理后台首页

**依赖：** T0.2

---

### T0.4 多租户基础设施

**目标：** 所有店铺级接口自动按 shopId 隔离

| 子任务 | 说明 |
|--------|------|
| TenantMiddleware | 从 JWT 提取 shopId 注入 request |
| Prisma 租户拦截器 | 自动为查询注入 where: { shop_id } |
| ShopContext Decorator | `@CurrentShop()` 装饰器快速获取当前店铺 |
| 前端 shopId 注入 | 登录后 localStorage/cookie 存储 shopId |

**验收标准：**
- [ ] 店铺 A 的员工请求接口，只能看到店铺 A 的数据
- [ ] 直接请求其他 shopId 的数据返回空结果（非报错）

**依赖：** T0.3

---

### T0.5 License 校验框架

**目标：** License Guard 可拦截未授权/过期请求

| 子任务 | 说明 |
|--------|------|
| License Module | 读取/校验/缓存 License |
| License Guard | 检查模块权限、员工数限制、到期时间 |
| License API | GET `/api/v1/license` 返回当前授权信息 |
| 前端 License 提示 | 到期提醒横幅、功能未授权弹窗 |

**验收标准：**
- [ ] License 过期时，除基础收银外的接口返回 403
- [ ] 员工数超限时，创建员工接口返回提示
- [ ] 前端显示当前版本和到期时间

**依赖：** T0.4

---

## Phase 1：会员 + 充值（Week 3-5）

### T1.1 会员等级管理

**目标：** 店主可自定义等级名称和折扣

| 子任务 | 说明 |
|--------|------|
| MemberLevel CRUD API | 创建/修改/删除/排序等级 |
| 等级管理页面 | 列表 + 新增/编辑弹窗，拖拽排序 |
| 默认等级逻辑 | 新会员自动关联排序第一的等级 |

**验收标准：**
- [ ] 可创建等级 "金卡，8.8 折"
- [ ] 删除有关联会员的等级时提示不可删除
- [ ] 新注册会员自动关联默认等级

**依赖：** T0.4

---

### T1.2 会员档案管理

**目标：** 前台可查询、新建、编辑会员

| 子任务 | 说明 |
|--------|------|
| Member CRUD API | 创建/查询/修改会员，支持手机号/姓名搜索 |
| 会员列表页 | 搜索框 + 列表 + 分页（桌面端表格、移动端卡片） |
| 会员详情页 | 基本信息、余额、消费记录、标签 |
| 新建/编辑会员弹窗 | 姓名、手机号、性别、生日、等级选择 |
| 会员卡号自动生成 | 按规则生成唯一卡号 |
| 手机端会员查询 | `/m/members` 快速搜索 + 新建 |

**验收标准：**
- [ ] 手机号搜索可实时模糊匹配
- [ ] 新建会员自动生成卡号
- [ ] 会员详情页展示余额和最近消费记录

**依赖：** T1.1

---

### T1.3 会员标签

**目标：** 店主可自定义标签，为会员打标签

| 子任务 | 说明 |
|--------|------|
| 标签组 + 标签 CRUD API | 创建标签组、组内标签 |
| 标签管理页面 | 标签组列表，组内标签增删 |
| 会员标签设置 | 会员详情中可添加/移除标签 |
| 系统自动标签 | 新会员（注册 < 30天）、沉睡会员（> 90天未到店） |

**验收标准：**
- [ ] 可创建标签组"客户偏好"，下设"喜欢短发""怕热"等标签
- [ ] 会员详情页可添加多个标签
- [ ] 沉睡会员自动标记

**依赖：** T1.2

---

### T1.4 充值方案管理

**目标：** 店主可配置充值方案

| 子任务 | 说明 |
|--------|------|
| RechargePlan CRUD API | 创建/修改/删除/上下架充值方案 |
| 充值方案管理页 | 方案列表，支持直充/充赠/阶梯/限时活动 |
| 限时活动状态 | 自动根据 startsAt/endsAt 显示进行中/已结束 |

**验收标准：**
- [ ] 可创建"充 1000 送 100"方案
- [ ] 限时活动到期自动标记为已结束
- [ ] 已下架方案不在充值时展示

**依赖：** T0.4

---

### T1.5 充值操作

**目标：** 前台可为会员充值，记录流水

| 子任务 | 说明 |
|--------|------|
| 充值执行 API | `POST /members/:id/recharge`，更新本金/赠送余额 |
| 充值页面（会员详情内） | 选择方案 → 确认金额 → 记录线下支付方式 → 完成 |
| 充值流水列表 | 会员详情内展示充值历史 |
| 审计日志记录 | 充值操作写入 AuditLog |

**验收标准：**
- [ ] 充值 1000 送 100 后，会员本金 +1000、赠送 +100
- [ ] 充值流水包含时间、操作人、金额、支付方式
- [ ] 审计日志可追溯

**依赖：** T1.2, T1.4

---

## Phase 2：服务项目 + 收银（Week 6-8）

### T2.1 服务项目管理

**目标：** 店主可自定义服务分类和项目

| 子任务 | 说明 |
|--------|------|
| ServiceCategory CRUD API | 分类增删改 |
| ServiceItem CRUD API | 项目增删改、上下架、排序 |
| 服务项目管理页面 | 左侧分类列表 + 右侧项目列表，拖拽排序 |
| 移动端项目选择组件 | 收银开单时使用的项目选择器 |

**验收标准：**
- [ ] 可创建"剪发类"分类，下挂"男士精剪 ¥60"
- [ ] 项目上下架后，收银开单只显示上架项目
- [ ] 移动端项目选择器按分类折叠展示

**依赖：** T0.4

---

### T2.2 员工管理

**目标：** 店主可管理员工账号

| 子任务 | 说明 |
|--------|------|
| Staff CRUD API | 添加/修改/停用员工，重置密码 |
| 员工列表页 | 姓名、角色、手机号、状态 |
| 新增/编辑员工 | 基本信息 + 角色选择 + 初始密码 |
| License 员工数检查 | 创建员工时校验 staffLimit |

**验收标准：**
- [ ] 可添加员工并指定角色（前台/发型师）
- [ ] 员工数达到 License 上限时提示升级
- [ ] 停用员工后该账号无法登录

**依赖：** T0.4, T0.5

---

### T2.3 收银开单 — 创建订单

**目标：** 前台可为会员开单，选择服务和员工

| 子任务 | 说明 |
|--------|------|
| Order 创建 API | 创建订单 + 订单项，每项关联服务员工 |
| 收银开单页面（桌面端） | 左侧会员信息 + 中间项目选择 + 右侧订单摘要 |
| 收银开单页面（移动端） | `/m/pos`，步骤式：选会员 → 选项目+员工 → 确认 |
| 服务员工选择 | 每个服务项目可指定服务员工 |
| 挂单功能 | 保存为 PENDING 状态，挂单列表可恢复 |
| 会员等级折扣自动计算 | 选择会员后，自动按等级折扣计算价格 |

**验收标准：**
- [ ] 创建订单包含多个服务项目，每项记录服务员工
- [ ] 金卡会员 8.8 折自动应用
- [ ] 挂单后可在挂单列表中恢复结算
- [ ] 移动端收银流程顺畅，3 步内完成开单

**依赖：** T1.2, T2.1, T2.2

---

### T2.4 收银开单 — 结算

**目标：** 订单可结算，支持余额/次卡/线下支付

| 子任务 | 说明 |
|--------|------|
| 结算 API | `POST /orders/:id/settle`，事务内完成支付扣减 |
| 余额支付 | 先扣赠送余额，再扣本金，余额不足时拒绝 |
| 次卡核销 | 检查有效期，扣减剩余次数 |
| 线下支付记录 | 记录支付方式和金额，不涉及资金流转 |
| 混合支付 | 余额 + 线下组合，部分扣余额部分记线下 |
| 优惠券抵扣 | 校验优惠券有效性，扣减优惠金额 |
| 订单撤销 | 当日可撤销，隔日不可 |
| 结算页面 | 支付方式选择、金额明细、确认结算 |
| 支付记录展示 | 订单详情中显示各支付方式明细 |

**验收标准：**
- [ ] 余额 ¥200 + 线下 ¥134.4 的混合支付正确记录
- [ ] 赠送余额 ¥50 先于本金 ¥150 被扣减
- [ ] 过期次卡无法核销
- [ ] 撤销订单后会员余额恢复
- [ ] 订单详情展示支付构成

**依赖：** T2.3

---

### T2.5 次卡管理

**目标：** 可为会员购买次卡，收银时核销

| 子任务 | 说明 |
|--------|------|
| PassCard 购买 API | 为会员创建次卡，记录购买价格和有效期 |
| 次卡列表 | 会员详情中展示已有次卡和剩余次数 |
| 次卡核销 | 结算时选择次卡支付，扣减次数 |
| 过期提醒 | 次卡列表标记已过期 |

**验收标准：**
- [ ] 可购买"剪发 10 次卡"
- [ ] 结算时选择次卡，扣减 1 次
- [ ] 过期次卡标记为不可用

**依赖：** T1.2, T2.4

---

### T2.6 优惠券管理

**目标：** 店主可创建优惠券，发放给会员

| 子任务 | 说明 |
|--------|------|
| CouponTemplate CRUD API | 创建满减/折扣券模板 |
| 优惠券发放 | 批量发放给指定会员 |
| 会员优惠券列表 | 会员详情展示可用/已用/过期券 |
| 结算时使用优惠券 | 选择优惠券，校验门槛，抵扣金额 |

**验收标准：**
- [ ] 可创建"满 200 减 30"券
- [ ] 发放后会员详情可见
- [ ] 结算时选择优惠券，金额自动扣减

**依赖：** T1.2, T2.4

---

## Phase 3：数据分析 + 员工统计（Week 9-10）

### T3.1 数据面板 — 看板

**目标：** 首页展示核心经营指标

| 子任务 | 说明 |
|--------|------|
| Dashboard API | 聚合查询：营业额、客流量、客单价、新增会员 |
| 看板页面（桌面端） | 4 个核心指标卡片 + 近 7 日趋势折线图 |
| 看板页面（移动端） | 紧凑卡片布局 |
| 时间范围切换 | 今日/本周/本月/自定义 |

**验收标准：**
- [ ] 首页 4 个指标数字正确
- [ ] 趋势图可按日/周/月切换
- [ ] 移动端适配正常

**依赖：** T2.4

---

### T3.2 数据面板 — 收入分析

**目标：** 可查看收入构成和项目热度

| 子任务 | 说明 |
|--------|------|
| Revenue API | 收入构成（线下/余额/充值/次卡）+ 项目排行 |
| 收入分析页面 | 饼图（收入构成）+ 表格（项目 TOP 排行） |
| 充值收入 vs 消费收入 | 区分当期充值和当期消费 |

**验收标准：**
- [ ] 饼图展示各支付方式占比
- [ ] 项目排行按消费次数降序

**依赖：** T3.1

---

### T3.3 数据面板 — 会员分析

**目标：** 可查看会员分布和趋势

| 子任务 | 说明 |
|--------|------|
| Member Analytics API | 等级分布、充值消费趋势、沉睡会员数 |
| 会员分析页面 | 等级分布饼图 + 消费趋势折线 + 沉睡会员预警 |

**验收标准：**
- [ ] 等级分布按自定义等级展示
- [ ] 沉睡会员（90天未到店）数量正确

**依赖：** T3.1

---

### T3.4 员工服务统计

**目标：** 可查看每位员工的服务次数和类型分布

| 子任务 | 说明 |
|--------|------|
| Staff Service Stats API | 按员工统计服务次数、服务类型分布、营收 |
| 员工统计页面 | 员工排行 + 单员工详情（类型分布表格） |
| 个人服务记录（发型师端） | 发型师登录后查看自己近期的服务记录 |

**验收标准：**
- [ ] 员工排行按服务次数降序
- [ ] 点进员工详情可看到剪发 X 次、烫染 X 次
- [ ] 发型师角色只能看自己的数据

**依赖：** T2.4, T2.2

---

### T3.5 操作日志

**目标：** 关键操作可追溯

| 子任务 | 说明 |
|--------|------|
| AuditLog 记录 | 充值/结算/撤销/等级变更/权限变更自动记录 |
| 日志查询 API | 按时间/操作类型/操作人筛选 |
| 日志查看页面 | 管理后台设置页内，列表展示 |

**验收标准：**
- [ ] 充值操作后 AuditLog 有记录
- [ ] 可按操作类型筛选日志

**依赖：** T0.4

---

## Phase 4：平台管理 + License（Week 11-12）

### T4.1 平台管理员认证

**目标：** 平台管理员有独立的登录和管理后台

| 子任务 | 说明 |
|--------|------|
| PlatformAdmin 模型 | 独立的管理员表，区分 SUPER_ADMIN/ADMIN/OPERATOR |
| 平台登录 API | `POST /platform/auth/login`，JWT 中 `type: 'platform'` |
| 平台登录页面 | `/platform/login` |
| 平台路由守卫 | 前端路由守卫 + 后端 Guard，仅平台管理员可访问 |

**验收标准：**
- [ ] 平台管理员可登录 `/platform`
- [ ] 店铺员工无法访问 `/platform/*` 页面和 API

**依赖：** T0.3

---

### T4.2 平台 — 店铺管理

**目标：** 平台管理员可管理所有租户店铺

| 子任务 | 说明 |
|--------|------|
| Shop CRUD API (平台级) | 创建/查看/修改/暂停/归档店铺 |
| 店铺列表页 | 搜索 + 状态筛选 + 店铺数统计 |
| 店铺详情页 | 基本信息、License 状态、员工数、会员数、最近活跃时间 |
| 创建店铺流程 | 填写店铺信息 → 自动创建店主账号 → 初始 License |

**验收标准：**
- [ ] 可创建新店铺并生成店主账号
- [ ] 暂停店铺后该店员工无法登录
- [ ] 店铺详情展示会员数、最近活跃时间

**依赖：** T4.1

---

### T4.3 平台 — License 管理

**目标：** 平台管理员可为各店铺分配和管理 License

| 子任务 | 说明 |
|--------|------|
| License 分发 API | 为店铺创建/更新 License |
| License 管理页面 | 各店铺 License 状态列表、到期时间、版本 |
| License 签发 | RSA 签名生成 License Key |
| 到期预警 | 平台总览中高亮即将到期（15天内）的店铺 |
| 续费/升级操作 | 选择新版本和时长，更新 License |

**验收标准：**
- [ ] 可为店铺分配专业版 License，有效期 1 年
- [ ] License 过期的店铺登录时看到降级提示
- [ ] 平台总览正确显示即将到期的店铺

**依赖：** T4.2, T0.5

---

### T4.4 平台 — 数据总览

**目标：** 平台管理员可查看跨店铺运营数据

| 子任务 | 说明 |
|--------|------|
| Platform Overview API | 全平台店铺数、活跃店铺、总营收、总会员数 |
| 平台总览页面 | 核心指标卡片 + 店铺营收排行 + 新增店铺趋势 |
| 各店铺使用量统计 | 员工数、会员数、订单量、存储用量 |

**验收标准：**
- [ ] 总览页面展示全平台汇总数据
- [ ] 可按店铺查看单店经营数据
- [ ] 活跃店铺（当月有订单）数正确

**依赖：** T4.2

---

## Phase 5：联调 + 上线准备（Week 13-14）

### T5.1 端到端业务验证

**目标：** 核心业务流程全链路可走通

| 子任务 | 说明 |
|--------|------|
| 新客户到店全流程 | 注册 → 充值 → 开单 → 结算 → 查看记录 |
| 老客户充值全流程 | 查询会员 → 充值 → 余额变动 |
| 混合支付验证 | 余额 + 线下 + 优惠券组合支付 |
| 多租户隔离验证 | 两个店铺数据互不可见 |
| License 限制验证 | 免费版功能限制 + 员工数限制 |
| 平台管理全流程 | 创建店铺 → 分配 License → 查看数据 |

**验收标准：**
- [ ] 6 条核心业务流程全部走通，无阻断 Bug

**依赖：** 所有前置任务

---

### T5.2 响应式适配验证

**目标：** 手机/Pad/桌面三种尺寸体验正常

| 子任务 | 说明 |
|--------|------|
| 手机端收银流程 | `/m/pos` 全流程操作 |
| Pad 端管理后台 | 横屏下管理后台布局正常 |
| 桌面端完整功能 | 管理后台 + 收银 + 数据面板 |

**验收标准：**
- [ ] iPhone SE / iPhone 15 / iPad / 1440p 桌面四种尺寸核心页面无溢出

**依赖：** T5.1

---

### T5.3 部署与 CI/CD

**目标：** 代码可自动部署到服务器

| 子任务 | 说明 |
|--------|------|
| Dockerfile (web + server) | 生产镜像构建 |
| Docker Compose (生产) | Nginx + Next.js + NestJS + PostgreSQL + Redis |
| GitHub Actions | lint → test → build → deploy |
| Nginx 配置 | SSL + 反向代理 + 静态资源缓存 |
| 环境变量管理 | `.env.production` 模板 |

**验收标准：**
- [ ] git push 后自动构建部署
- [ ] HTTPS 正常，API 可访问
- [ ] 前端页面加载正常

**依赖：** T5.1

---

## 任务依赖关系图

```
T0.1 脚手架
 ├── T0.2 数据库
 │    └── T0.3 认证
 │         └── T0.4 多租户
 │              ├── T0.5 License
 │              ├── T1.1 会员等级
 │              │    └── T1.2 会员档案
 │              │         ├── T1.3 标签
 │              │         └── T1.5 充值操作 ← T1.4 充值方案
 │              ├── T2.1 服务项目
 │              ├── T2.2 员工管理 ← T0.5
 │              └── T3.5 操作日志
 │
 T2.3 开单 ← T1.2, T2.1, T2.2
 └── T2.4 结算
      ├── T2.5 次卡
      ├── T2.6 优惠券
      ├── T3.1 看板
      │    ├── T3.2 收入分析
      │    └── T3.3 会员分析
      └── T3.4 员工统计 ← T2.2

 T4.1 平台认证 ← T0.3
 └── T4.2 平台店铺
      ├── T4.3 平台License ← T0.5
      └── T4.4 平台总览

 T5.1 E2E验证 ← 全部
 └── T5.2 响应式
 └── T5.3 部署
```

---

## 工期估算

| Phase | 周数 | 任务数 | 说明 |
|-------|------|--------|------|
| Phase 0 基础架构 | 2 周 | 5 个 | 后端 3 + 前端 1 + DevOps 1 |
| Phase 1 会员充值 | 3 周 | 5 个 | 会员体系 + 充值流水 |
| Phase 2 服务收银 | 3 周 | 6 个 | 核心交易流程，最重 |
| Phase 3 数据分析 | 2 周 | 5 个 | SQL 聚合 + 图表 |
| Phase 4 平台管理 | 2 周 | 4 个 | 多租户管控 |
| Phase 5 联调上线 | 2 周 | 3 个 | 集成验证 |
| **合计** | **14 周** | **28 个** | 约 3.5 个月 |

---

## 优先级排序（MVP 最小集）

如果需要进一步压缩，以下是 MVP 最小集（Phase 0-2，约 8 周）：

| 必须（MVP） | 可延后 |
|-------------|--------|
| T0.1-T0.4 基础架构 | T0.5 License 框架 |
| T1.1 会员等级 | T1.3 标签 |
| T1.2 会员档案 | |
| T1.4-T1.5 充值 | |
| T2.1 服务项目 | |
| T2.2 员工管理 | |
| T2.3 开单 | T2.5 次卡 |
| T2.4 结算（余额+线下） | T2.6 优惠券 |
| | T3.x 数据分析 |
| | T4.x 平台管理 |
| | T5.x 联调上线 |

**MVP 可用标准：** 一个新客户进店 → 注册会员 → 充值 → 开单选项目指定员工 → 余额/线下支付结算 → 完成订单。
